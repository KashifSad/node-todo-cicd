apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-app:62
spec:
  selector:
    matchLabels:
      app: flask-app:62
  template:
    metadata:
      labels:
        app: flask-app:62
    spec:
      containers:
      - name: flask-app:62
        image: kashifsadiq/flask-app:62
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 5000
